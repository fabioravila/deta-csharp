
namespace DetaCSharp.Constants
{
    public static class BaseApi
    {
        const string PUT_ITEMS = "/items";
        const string QUERY_ITEMS = "/query";
        const string INSERT_ITEMS = "/items";
        const string GET_ITEMS = "/items/:key";
        const string PATCH_ITEMS = "/items/:key";
        const string DELETE_ITEMS = "/items/:key";
    }
}
